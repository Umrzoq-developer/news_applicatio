import React from "react";
import {connect} from 'react-redux';

const GeneralPage = () => {
    return(
        <div>
            GeneralPage
        </div>
    )
};

export default connect()(GeneralPage)
